dependencies {
    compile("com.everteam.cloud:everteam-cloud-starter-client")
    
    testCompile group: 'org.springframework', name: 'spring-test', version: '4.0.5.RELEASE'
    testCompile("junit:junit:4.12")
	
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.8'
}
